{
  "name": "churchos",
  "version": "0.2.1",
  "private": true,
  "scripts": {
    "dev": "cd apps/web && node server.js dev",
    "build": "cd apps/web && npx next build",
    "start": "cd apps/web && node server.js",
    "install:all": "npm install && cd apps/web && npm install"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
